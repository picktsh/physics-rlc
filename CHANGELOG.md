# Changelog

本项目所有值得关注的改动记录于此，遵循 Keep a Changelog 精神。
只描述「做了什么、为什么」，不含文件路径 / 数值 / 代码细节。

## [未发布]

### 新增
- **路由化架构**：引入前端路由，以哈希模式适配无服务端重写的静态托管；建立路由表与访问校验守卫，并用单一导航数据源驱动路由、侧栏目录与首页入口三处同源。
- **外壳布局**：重构为全宽粘性页眉 + 可折叠左目录 + 居中主内容 + 全宽页脚的统一外壳；移动端目录转为抽屉，其余与桌面保持一致。
- **页面拆分与保活**：将原单一首页拆分为独立功能视图与落地页；对含长耗时后台任务（扫频）的页面做跨路由保活，避免切换时丢失进度。
- **站点标识与二维码**：重设计站点 logo 并作为单一来源复用于浏览器图标、页眉与二维码中心；侧栏提供自适应生成的站点二维码，桌面与移动端均可扫码分享。
- **新增配色主题**：在既有主题体系上增加一套品牌绿配色，浅色模式下表单控件保持亮色，画布取色随主题自动联动。
- **电路 / 阻尼实验页板块全屏**：把「放大观察」升级为整块工作区的全屏编辑模式，元件库与画布一同放大并盖住外围布局与菜单，便于从容排列元件；进入全屏自动隐藏参数编辑栏，退出或按 Esc 即恢复，且全屏保持各端原有的响应式排布。
- **导入示例**：电路与阻尼实验页新增一键导入预置电路布局，内置串联 RLC 的欠阻尼 / 临界阻尼 / 过阻尼三种典型示例，免去每次手动拖放与接线。

### 变更
- **UI 图标统一**：将界面中的 emoji 与零散手写图标替换为标准图标库组件；画布字符、装饰性 emoji 与领域手绘图形不在范围内。随之卸载不再被引用的图标依赖。
- **样式分层**：把巨型全局样式按变量 / 基础 / 组件分层拆分，入口仅做聚合；清理全项目零引用的死样式类，按钮与卡片交互态统一交给 UI 库。
- **左目录折叠语义**：折叠行为修正为「展开显示图标 + 文字 ⇄ 收起仅图标导轨」的原生语义，并保证页眉汉堡按钮与侧栏图标始终共轴、折叠展开时图标零横向位移。
- **导航项样式下沉**：目录项样式改由原子化样式在组件内实现，统一为一致的网格间距，组件本身保持无状态。
- **搭建页工具栏整合**：阻尼页把板块操作按钮与 3D 视角按钮合并为同一行并支持换行；两页按钮统一为一致的尺寸与语义配色，清空等破坏性操作改用警示色，操作提示移至板块标题下方。
- **界面层级体系**：为全站浮层建立分档的层级规范并收敛为单一数据源，页面级全屏遮罩明确压在组件库浮层之下，修复此前高层级遮罩盖住下拉、弹窗等组件浮层的问题。

### 移除
- 清理未被引用的静态资源。

### 修复
- **构建告警**：在构建配置中过滤上游依赖自身产物引发的注释位置误报，不影响最终产物。
