# Changelog

> 本项目所有值得关注的改动记录于此，按 [Keep a Changelog 1.1.0](https://keepachangelog.com/zh-CN/1.1.0/) 规范分类，最新在上。
> 站点提交即发布，不编语义化版本号：条目按发布日期（YYYY-MM-DD）归组，未发布的改动置于顶部 `[Unreleased]`。
> 写给人看，不是机器 diff：每条只回答「改了什么、为什么、还差什么」，不展开文件路径与代码细节（存量条目不追溯改写）。运行方式与现行架构见 README 及相关文档。

## 2026-09-23

### 新增
- **元器件演练场（隐藏页）**：新增一个不进菜单、与教学业务无关的预览页，集中展示每个元件的 2D 符号与 3D 缩略图，并提供可自由拖放搭建的 3D 沙盒，用于验收元件视觉全家桶与排查回归。
- **3D 拖放落点实时预览**：在 3D 台面选中或拖入元件时，落点处实时显示半透明的元件预览，取代原先“选中后弹一行提示文字”导致的画布上下闪动；桌面拖拽与移动端点选均生效。

### 变更
- **元器件视觉资产收敛为单一数据源（重构第一阶段）**：元件元数据、2D 符号、2D 画布绘制器、3D 建模统一收进一套“元件注册表”，电路搭建页与阻尼实验页的元件库改为引用同一来源，删除两处大量重复的内联定义与绘制代码，为后续统一编辑器与数据格式打地基；搭建与仿真行为保持不变。尚待四主题视觉走查验收；数据层（稳定 ID / 导入导出 / 历史迁移）、两页编辑器合并与就地上下文菜单等交互优化列入重构文档待办。
- **单位体系全站换轨（零换算）**：存储、显示、输入三层单位彻底一致——电感直接以亨利（H）存储、频率统一以千赫（kHz）为准（含收音机工程页），不再有任何页面内换算层；国际单位制换算只发生在物理计算层一处。此前「同一数值在不同页各算各的」的口径隐患根除；本地旧单位历史记录随存储版本升级自动废弃。
- **物理量精度全站归一**：电阻 / 电感 / 电容 / 电压 / 电流 / 频率 / 相位 / 品质因数 / 误差百分比等各量的单位、小数位与输入步长登记进单一总表，输入框、表格、画布标注、CSV 导出一律从此取数：电流 4 位防失谐点截断、电压 2 位、相位 1 位、Q 值 2 位、误差 2 位；数字旁的单位标签也全部改为从总表取词，改口径只改一处。
- **数值表格全面迁移 UI 库数据表**：实验数据记录、仿真 / 实测历史、被剔除异常点明细及实测数据录入表共六处，统一换用数据表组件——数值列右对齐、表头粘顶、窄屏横向滚动，移动端可读性显著提升。
- **幅频图通频带标注重排**：f₁/f₂ 与标记点平齐分居带外侧，「通频带 / BW」竖排于带右方，半功率分式标注上移——窄带宽与移动端不再互相叠字、不压曲线；频率范围输入框逐键同步重绘图形（此前需失焦才生效）。

### 修复
- **加载仿真历史不出特性曲线**：电压最大值法历史记录点「加载」后参数已更新但仿真状态未标记，三大特性曲线始终停在占位图；现加载即恢复绘制。

## 2026-09-22

### 新增
- **电路搭建与数据分析双向跳转引导**：仿真成功后给出轻提示告知结果去向；搭建页工具栏新增常驻「数据分析」按钮，数据分析页标签行右侧常驻「去搭建电路」按钮。此前点击仿真零反馈、用户不知道去哪里看数据，现在两页之间一键互达，跳转入口不随提示消失。

## 2026-09-21

### 新增
- **路由切换入场动画**：切换页面时内容区淡入并轻微上移，遵循系统「减弱动效」偏好。
- **侧栏折叠过渡动画**：折叠时文字标签淡出、分组子项展开收起，替代原先的生硬切换。
- **数据分析页标签切换**：把电压最大值法、相位差判别法、LC 电压幅值法三种谐振判别法收进单个「数据分析」页，顶部标签一键切换；各方法仍保留原地址可直达，含后台扫频的方法在切换时进度不丢。

### 变更
- **串联谐振实验目录收敛**：该实验下侧栏由四个子项精简为「电路搭建 + 数据分析」两项，三种判别方法归入数据分析页内以标签切换。
- **元件参数编辑精度与单位**：电阻固定一位、电感三位、电容四位、信号源频率四位小数，并配置适配的键盘上下微调步长；电感显示单位由 mH 改为 H（仅显示层换算，仿真口径与结果不变）。
- **数值单位与精度单一数据源**：元件参数的单位 / 精度 / 步长与品质因数等结果展示的小数位收敛到一处统一维护，品质因数 Q 全站统一为两位小数。
- **颜色主题收敛为单一数据源**：全站配色统一为一套语义色板变量，页面样式、UI 库主题、画布强调色三处从此同源；此前那层用高优先级选择器「把硬编码色事后反查回变量」的临时适配层（约 270 行，漏登记即不跟主题）已彻底移除，组件改为直接引用语义色，允许少量视觉微调。
- **表单控件全面改用 UI 库组件**：各实验页手写的原生按钮 / 输入框 / 下拉 / 滑块 / 开关 / 勾选替换为 UI 库对应件，实心状态色按钮改由组件的语义类型（主色 / 成功 / 警告 / 危险）表达，天生跟随主题并清除写死的状态色；固定深色的终端风输入、示波器底、主题预览点等按允许清单保留。按钮观感再统一为 `secondary` 变体 + 默认尺寸（去除零散的 tiny/small），圆形图标钮与紧凑覆盖层保留特例。
- **数值输入统一与规范化**：实测数据录入、元件参数、信号源波形、仿真参数、坐标轴范围等数值输入统一为数字输入组件，单位与变量符号内联为前后缀，输入即时生效并统一小数精度；此前手写的「失焦兜底 + 输入中间态缓存」样板逻辑随之删除。
- **间距与字号全站归一**：间距档位收敛（大间距统一 16px、主内容区单独、消除按断点的间距抖动），字号去除奇数 / 超小 / 任意 px 一律就近归到偶数档、与默认同值的 `text-sm` 移除，代码更整洁。
- **破坏性操作按钮补语义色**：清空 / 删除 / 重置等按钮统一带上危险 / 警示语义色。
- **原生弹窗温和化**：全站浏览器 `alert` / `confirm` 替换为 UI 库的轻提示与确认对话框。
- **公式展示统一**：块级与行内公式内边距规范化，并修复公式卡右侧误现的纵向滚动条。

### 修复
- **实测数据表格无法输入**：电压最大值法实测数据表格的数字输入框此前无法键入与更新，已修复。
- **路由切换致页面空白**：入场动画改用不依赖组件单根、也不破坏保活的实现，修复多根页面在切换动画下整页空白的问题。

## 2026-09-20

### 新增
- **串联谐振实验入口页与目录折叠分组**：电路搭建、电压最大值法、相位差判别法、LC 电压幅值法四个模块归拢到一个实验之下，侧栏父项仅负责开合子项，路由与首页卡片仍可直达。
- **文档中心**：新增独立功能页，内置操作手册并兼作通用 Markdown 查看器——支持标签页管理、拖入本地文档（含压缩包带图预览、公式与流程图渲染）、会话内暂存恢复，以及导出为原文 / 文档 / 图片 / 打印 PDF；导出物统一附署名水印。
- **实测数据计算 Q 值**：电压最大值法页支持从实测曲线按半功率点法求截止频率与带宽，进而得到品质因数，与理论值对照。
- **导入示例扩充**：预置电路新增「标准谐振」示例，与三种阻尼示例共同覆盖典型课堂场景。
- **心率检测电路图放大**：带通滤波器电路图支持放大全屏查看，按 Esc 或点击空白退出。
- **收音机调谐核心公式板块**：将调谐、选频、品质因数与带宽的公式成组呈现，与页面读数一一对应。

### 变更
- **页面更名**：「仿真分析」更名为语义更准确的「电压最大值法」，站内相关指引文案同步。
- **图表公式标注**：幅频特性图的半功率截止线标注改为数学分式排版，并跟随图表重绘实时定位。
- **开发环境**：固定开发服务器端口，避免多项目并行时端口漂移导致本地存储归属混乱。

### 修复
- **文档中心加载失败**：修正第三方 Markdown 公式插件在新一代构建器下的模块互操作解包，此前点击文档中心会因组件加载抛错而被路由静默中止导航。

## 2026-09-19

### 新增
- **路由化架构**：引入前端路由，以哈希模式适配无服务端重写的静态托管；建立路由表与访问校验守卫，并用单一导航数据源驱动路由、侧栏目录与首页入口三处同源。
- **外壳布局**：重构为全宽粘性页眉 + 可折叠左目录 + 居中主内容 + 全宽页脚的统一外壳；移动端目录转为抽屉，其余与桌面保持一致。
- **页面拆分与保活**：将原单一首页拆分为独立功能视图与落地页；对含长耗时后台任务（扫频）的页面做跨路由保活，避免切换时丢失进度。
- **站点标识与二维码**：重设计站点 logo 并作为单一来源复用于浏览器图标、页眉与二维码中心；侧栏提供自适应生成的站点二维码，桌面与移动端均可扫码分享。
- **新增配色主题**：在既有主题体系上增加一套品牌绿配色，浅色模式下表单控件保持亮色，画布取色随主题自动联动。
- **电路 / 阻尼实验页板块全屏**：把「放大观察」升级为整块工作区的全屏编辑模式，元件库与画布一同放大并盖住外围布局与菜单，便于从容排列元件；进入全屏自动隐藏参数编辑栏，退出或按 Esc 即恢复，且全屏保持各端原有的响应式排布。
- **导入示例**：电路与阻尼实验页新增一键导入预置电路布局，内置串联 RLC 的欠阻尼 / 临界阻尼 / 过阻尼三种典型示例，免去每次手动拖放与接线。

### 变更
- **UI 图标统一**：将界面中的 emoji 与零散手写图标替换为标准图标库组件；画布字符、装饰性 emoji 与领域手绘图形不在范围内。随之卸载不再被引用的图标依赖。
- **样式分层**：把巨型全局样式按变量 / 基础 / 组件分层拆分，入口仅做聚合；清理全项目零引用的死样式类，按钮与卡片交互态统一交给 UI 库。
- **左目录折叠语义**：折叠行为修正为「展开显示图标 + 文字 ⇄ 收起仅图标导轨」的原生语义，并保证页眉汉堡按钮与侧栏图标始终共轴、折叠展开时图标零横向位移。
- **导航项样式下沉**：目录项样式改由原子化样式在组件内实现，统一为一致的网格间距，组件本身保持无状态。
- **搭建页工具栏整合**：阻尼页把板块操作按钮与 3D 视角按钮合并为同一行并支持换行；两页按钮统一为一致的尺寸与语义配色，清空等破坏性操作改用警示色，操作提示移至板块标题下方。
- **界面层级体系**：为全站浮层建立分档的层级规范并收敛为单一数据源，页面级全屏遮罩明确压在组件库浮层之下，修复此前高层级遮罩盖住下拉、弹窗等组件浮层的问题。
- **视频资源体验**：放大播放的移动端兼容性修复（全屏方案回归社区共识），组件结构扁平化、样式原子化，并优化加载淡入与占位指示。

### 移除
- 清理未被引用的静态资源。

### 修复
- **构建告警**：在构建配置中过滤上游依赖自身产物引发的注释位置误报，不影响最终产物。

## 2026-09-18

### 变更
- **配色开关换位**：从侧栏 / 悬浮位置迁至页眉标题行右端，修复其遮挡 AI 助手窗口，并确保移动端各断点均可见。

### 修复
- **移动端适配**：触摸交互适配改用指针类型判定而非宽度断点，并对视频资源组件做性能优化。

## 2026-09-17

### 新增
- **多主题配色系统**：建立白 / 黑 / 马卡龙三套可切换配色，配色变量单一数据源，原子样式类经语义映射层随主题自动联动；图表与画布组件取色同步主题切换，并带过渡动画与防闪白处理。

## 2026-09-14

### 新增
- **AI 协作协议接入**：团队级 AI 协作契约随依赖安装自动同步到项目，保证多仓库约定单点维护。

## 2026-09-13

### 新增
- **视频资源增强**：接入抖音视频源并支持放大播放；电路搭建支持点击导线操作与信号源波形参数配置；新增首屏加载动画改善打开体验。

### 移除
- 下线独立的频率扫描功能模块，其能力已由保活的扫频页面覆盖。

### 修复
- 阻尼实验页画布光标跟踪错位与界面样式问题。

## 2026-09-12

### 新增
- **RLC 阻尼振荡特性实验**：独立实验页，支持拖拽搭建并观察欠阻尼 / 临界阻尼 / 过阻尼波形。
- **心率信号带通滤波**：工程应用页以有源 RC 带通滤波器实现心率信号处理演示。

## 2026-09-11

### 新增
- **心率检测模块**：实现串口采集与实时脉搏波形展示；工程应用组件升级为二级标签结构，心率检测作为独立子模块入驻。

## 2026-09-10

### 新增
- **视频资源页**：整理哔哩哔哩视频清单，播放器按需懒加载避免拖慢页面打开。

### 变更
- 统一应用蓝色主题基调，元件增加公差设置能力。

### 移除
- 精简侧栏底部的页脚内容与样式。

## 2026-09-05 ~ 2026-09-09

### 新增
- **3D 电路板**：电路搭建引入三维模型渲染与放大观察，实验画布升级为主内容区三栏布局并持续优化渲染性能。

## 2026-07-14

### 变更
- **访问验证升级**：静态密码改为动态验证码并随会话保持，验证入口结构重构；输入限定数字并默认唤起数字键盘。

## 2026-07-13

### 新增
- 应用访问密码验证，保护课堂演示环境入口。

## 2026-07-10

### 变更
- 统一各界面组件布局与样式规范。

## 2026-07-06

### 新增
- **豆包 AI 助手**：全站悬浮对话窗口，支持文件上传提问。
- **LC 电压幅值法图表**：补充 UL/UC 李萨如图并优化图表布局。

### 变更
- 全站半功率近似值统一替换为精确的 Imax/√2 表达式，涵盖计算逻辑、图表标注、公式展示与结果卡片。
- 优化移动端响应式布局与触摸交互体验。

## 2026-07-05

### 新增
- **公式原理模块**：实验原理与核心公式独立成页，分数统一为竖式排版；新增 LC 电压幅值法功能页。
- **电路并联节点**：点击导线中段即可创建分支节点实现并联连接，回路验证与参数提取改为基于实际连线拓扑。
- 元件拖拽兼容移动端触摸；接入豆包服务并重构聊天组件交互。

### 变更
- 状态管理迁移至 Pinia；Tab 导航以公式原理优先；统一调整电路元件默认参数。

### 修复
- 自动扫频暂停失效、幅频图不实时更新与小数输入问题；实测数据频率单位修正。

## 2026-07-04

### 新增
- 项目初始化；GitHub Actions 自动构建部署至 GitHub Pages，产物改用相对路径资源修复子路径托管 404。
