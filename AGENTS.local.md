# AGENTS.local —— physics-rlc 项目专属约定

> 补充根 `AGENTS.md`。此处只记录「不看会踩坑」的**需求、原则、限制与规范**，不复述代码、不写实现细节。改动相关功能前必读。

## 样式与组件组织（网站按页面/路由组织）
- 本项目是**具体网站而非公共组件库**：页面私有组件**就近放到对应 `src/views/<页面>/components/`**，路由级页面组件统一为 `src/views/<页面>/index.vue`；内部模块一律用 **`@/` 别名**引用，避免脆弱的深层相对路径。
- `src/components/` **只留跨页复用 / 全局外壳使用的公共件**：无业务语义的基础 UI 放 `components/ui/`；被两页以上共用的件、全局挂件各自成模块。**是否下沉 views 的唯一标准是真实复用面**，单页使用的一律下沉。
- **样式目标：全局 CSS 归零。** 表达优先级：① naive-ui 组件 → ② UnoCSS 既有原子类（**不自建项目专属 shortcut**，让开发者只需遵循标准 UnoCSS）→ ③ 组件 `<style scoped>`（sticky / 自定义属性 / calc / 纹理 / 深覆盖等原子做不到的）→ ④ 极简全局层（仅跨页且无法原子化的，如坐标纸底纹、naive 卡片头深覆盖）。
- **严格规范化**：新写 / 迁移的间距、尺寸、圆角对齐 **4px 网格**，写死色值替换为主题变量；**字体字号、细线描边（1/2/3px）、纹理单元等非布局间距不受网格约束**。改数值前评估视觉回归，拿不准保持原视觉。

## 第三方视频嵌入
- 外部短视频平台的嵌入播放器存在固定的最小尺寸、不支持小容器自适应；容器被压小时画面会被裁切或退回竖屏手机版。适配只能用**等比视觉缩放并保持完整画面（contain）**，不得靠压缩容器尺寸硬塞。
- 居中对齐依赖外层弹性布局，**禁用 auto 外边距**——嵌入盒大于容器时会因过约束而贴边跑偏。
- 嵌入参数遵循平台既有能力即可，已被验证无效的开关不再尝试。

## 移动端全屏 / 横屏
- 全屏与屏幕方向控制统一走框架已封装的组合式能力，**不手写原生 API**。
- 部分移动浏览器不支持对非原生视频元素发起全屏、也不提供方向锁定；此类环境必须保留一条**回退的放大展示路径**，回退不可删除。
- 全屏发起必须发生在用户点击手势的同步调用栈内；横屏锁定属尽力而为、失败静默；**禁止用 CSS 旋转伪装横屏**。

## 触屏适配
- 交互差异一律按**能力检测**（是否支持悬停）区分，**不用视口宽度断点**判断触屏与否。

## 弹窗与遮罩
- 全局采用直角主题时，个别需要圆角的组件单独恢复，不受全局牵连。
- 覆盖第三方组件里写死的样式（如遮罩色）时，**必须把作用域限定在本场景容器**，避免污染其他同类弹窗。
- 弹性容器内的文本省略需保证其允许收缩，否则省略号不生效。

## 界面层级（z-index）分区
- 全站浮层层级走变量层的 `--z-*` 令牌分档，**单一数据源**；组件内**禁止写裸 `z-index` 数值**，一律复用令牌。
- 由低到高五档：背景装饰 < 卡片内局部堆叠 / tooltip < 页眉 / 侧栏等外壳 < 全局浮窗（AI 助手气泡）< 板块全屏 / 页面级遮罩。**最顶永远留给组件库浮层（弹窗 / 下拉 / 气泡 / 消息，库自身从更高基线递增），业务层级不得越过它**。
- 新增浮层先判断落在哪一档再取对应令牌；页面级全屏遮罩务必压在组件库浮层之下，否则会被自己触发的下拉 / 弹窗反向遮挡。
- 原子化样式里以任意值形式引用令牌（如 `z-[var(--z-…)]`）可正常产出，`z-` 前缀不存在描边类 `var()` 的宽度 / 颜色歧义。

## 电路 / 阻尼实验页：板块全屏与工具栏
- 「放大」语义 = **整块工作区板块级全屏**（元件库 + 画布 + 操作一起放大、盖住页眉 / 侧栏 / 浮窗），而非仅画布放大；进入后隐藏右侧参数编辑栏，退出或按 Esc 恢复。
- 全屏**只铺满视口并保留该区块既有响应式布局**，不得强制改变栅格方向：桌面元件库保持在左，窄屏元件库退到顶部并改为横向滚动一排。
- 工具栏按钮**常显**（不随是否已放入元件而出现 / 消失），避免操作元件引起布局偏移；操作提示小字放在板块标题下方，与按钮行分开。
- 3D 画布自带视角按钮（自动旋转 / 复位视角）；板块级操作按钮通过画布组件的具名插槽注入到**同一行**（支持换行）。两页按钮统一用组件库默认尺寸与语义色：主操作用主色、清空 / 删除等破坏性操作用警示 / 危险色。
- 「导入示例」的预置电路数据为两页共用的单一来源，只描述元件摆放与连线拓扑，两页各自按自身落数据方式应用。

## 结构与抽象
- 仅在存在**两个及以上真实消费者**时才抽离公共单元；单消费者逻辑内联回原处。
- 注释只写「不看会踩坑的约束 / 口径来源」；方案选型与踩坑经验写进本文件，不留在代码注释里答辩。

## 路由与导航
- 路由表、侧栏目录、首页入口三处**必须同源**，增改页面只动单一数据源。
- 部署在无服务端重写的静态托管上，路由**必须用哈希模式**，否则刷新 / 直达会 404。
- 访问校验（门）作为**路由守卫**实现，而非视图内的条件渲染切换。
- 需要跨路由保活的页面，其命名必须与保活名单严格一致，否则丢失运行状态。

## UI 图标
- 界面图标统一使用既有矢量图标库组件，**禁止用 emoji 或手写内联 SVG 充当 UI 图标**（按钮前缀、增删改、状态切换等）。
- 例外（不算 UI 图标，勿动）：画布绘制字符、正文 / 标题的装饰性 emoji、领域相关的手绘电路 / 波形图形。
- 引用图标名以图标库实际存在的导出为准，用前核对，不臆造。

## 设计基准
- **4px 网格**：间距 / 尺寸对齐 4 的倍数，不引入非网格像素值。
- **间距收敛**：紧凑小间距（≤12px）保留；**≥16px 的 padding/margin/gap 一律归一到 16px**；主内容区 `main` 单独（移动端 16px、桌面 上16/左右40/下40）。**禁止按断点的间距抖动**——`p-2 sm:p-3` 这类响应式尺寸对塌缩为单值（对齐 / 换行类如 `sm:ml-auto`、`flex-wrap`、流体 `clamp()` 不受此限）。
- **圆角**：默认 8px，特别小的标签 4px，胶囊 / 圆形徽标另论。
- **字号**：body 默认 14px，常规文本不写字号类；小字下限 12px；**禁止 9/10/11px、奇数与任意 px 字号**（就近归 12/14/16），与默认同值的 `text-sm` 直接删。
- **慎用边框**：容器描边占盒内布局尺寸、需逐个细调，能不画就不画；区块分隔优先「不同底色」或「阴影」。例外：标题左侧 accent bar 属自身框内标记、不推挤兄弟，不受限。

## 主题与配色（颜色 token 单一数据源）
- 配色走数据属性驱动的多主题体系；新增一套配色须同步全部消费触点（变量、切换项、组件主色、首屏防闪白、切换器预览），保持同源。
- **颜色一律走 `--app-*` 语义 token，禁止硬编码色 / 标准灰阶类**。曾有约 270 行「事后把硬编码色反查回变量」的高特异性适配层，脆弱（漏登记即不跟主题），已彻底删除；组件模板一律直接引用 token 类。⚠ `text-`/`border-`/`divide-`/`ring-` 用 `var()` 须带 `color:` 类型提示（`bg-` 可省）。
- token 命名 `--app-` 前缀 + 扁平化（无中间类别词）；状态色词表对齐 naive-ui（用 `error` 不用 `danger`）。**完整 token 清单以 `theme.css` 为准，不在此复述**；带品牌语义的主题主色（如 green 取 Vue 品牌绿）属设计口径。
- **naive-ui 主色 / 状态色运行时从 `--app-*` 读取**，与 CSS 变量、画布强调色同源，不维护 JS 常量映射表。
- **固定深色 UI 允许清单**（不套 token、不参与主题，改前先确认场景）：终端风输入、示波器 / 画布固定底及 canvas 内部绘制色、`index.html` 内联 loading 色（改 token 时须同步）、主题预览圆点、深色底刻意白字。
- 画布 `--canvas-*` 保持独立命名（运行时读取，功能必需）；透明 canvas 由 theme.css 兜底「图纸底」。

## 组件与表单
- **表单控件统一用 naive-ui 组件**（自带主题色、天然跟主题），不再手写原生 `<button>` / `<input>` 配样式。数值输入用 `NInputNumber`，单位 / 变量符号走其 `#prefix` / `#suffix`，精度按需 `:precision`；控件上不带宽度等布局类，尺寸由外层元素约束。
- **按钮观感大一统**：普通带标签按钮统一 `secondary` 变体 + 默认尺寸，状态靠 `type` 语义区分（破坏性→`error`、主操作→`primary`），禁 `tertiary` 与零散 `size`。有意例外：圆形图标钮、chat 大圆钮 / 页眉 quaternary、覆盖层角标、`text` 链接式行内操作、`n-alert`。
- **紧凑工具条**（实验 / 图表页横向操作区）用纯 flex 而非 `NForm`——`NForm` 置顶标签会与无标签按钮错位；标签内联在控件左侧或走 prefix/suffix，滑块 `flex-1` 填满，尾控件 `ml-auto` 右对齐，移动端 `flex-wrap` + 主按钮 `w-full sm:w-auto`。

## 反馈与动效
- **禁用原生 `alert` / `confirm`**：一律用 naive 反馈——组件内 `useMessage` / `useDialog`（App.vue 已挂 Provider），store / composable 等非组件上下文用 `createDiscreteApi`。
- **路由入场动画不要用 `<Transition>`**：多数路由页是多根 fragment 且需 KeepAlive 保活，`<Transition mode="out-in">` 拿不到单元素会卡在 opacity:0 致整页空白。改由稳定 wrapper `<div>` 上「切 class + 强制回流」重放 CSS 动画（不 remount KeepAlive、不要求单根）。
- **侧栏折叠 / 分组过渡**：折叠宽度用 CSS `transition`，文字标签用 opacity 淡出（非 `display:none` 硬切），分组开合用 `NCollapseTransition`；全程严守图标共轴零位移（见「外壳与折叠共轴」）。

## 外壳与折叠共轴
- 左目录折叠语义为「展开显示图标 + 文字 ⇄ 收起仅剩图标导轨」的原生收窄，而非整条滑出视口。
- 外壳几何（自定义属性驱动的尺寸、折叠联动、页眉共轴 calc/clamp 内边距等无法原子化的规则）写在各 `layouts/*.vue` 的 `<style scoped>`，不再进全局组件层。
- **页眉汉堡按钮图标与侧栏导航图标必须共轴**，且折叠 ⇄ 展开过程中图标零横向位移；改动相关内边距或按钮尺寸时必须同步维持此约束，否则共轴失效。
- 折叠开关是页眉唯一入口，导轨内不放第二个展开控件。

## 构建
- 上游依赖自身产物引发的构建告警（非本站代码）在构建配置里统一过滤，**不改依赖、不逐个消除**。
